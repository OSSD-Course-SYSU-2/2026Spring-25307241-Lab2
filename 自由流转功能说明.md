# HarmonyOS 自由流转功能实现说明

## 功能概述

本项目已成功实现HarmonyOS的**自由流转**功能，包括：
- **跨端迁移**：应用可以在不同设备间无缝迁移
- **应用接续**：保持应用状态的连续性

## 实现内容

### 1. 配置文件修改 (module.json5)

已添加以下配置：

```json5
{
  "abilities": [{
    "continuable": true,  // 启用应用接续能力
    "launchType": "singleton"  // 单实例模式
  }],
  "requestPermissions": [{
    "name": "ohos.permission.DISTRIBUTED_DATASYNC",  // 分布式数据同步权限
    "reason": "$string:distributed_datasync_reason",
    "usedScene": {
      "abilities": ["EntryAbility"],
      "when": "inuse"
    }
  }]
}
```

### 2. EntryAbility 实现的核心接口

#### onContinue() - 保存迁移数据
- 在源端设备触发迁移时调用
- 保存当前页面路径和应用状态数据
- 返回 AGREE 表示同意迁移

#### onCreate() / onNewWant() - 恢复迁移数据
- 在目标端设备启动时调用
- 检查启动原因是否为迁移
- 恢复保存的应用数据

#### onWindowStageRestore() - 窗口恢复
- 迁移启动时触发（替代 onWindowStageCreate）
- 加载迁移前的页面
- 恢复UI上下文

## 使用条件

### 设备要求
- 两台设备都需要运行 **HarmonyOS 5.0** 或更高版本
- 支持的设备类型：手机、平板、2in1设备

### 网络要求
- 两台设备需要登录**同一个华为账号**
- 两台设备需要打开 **WLAN 和蓝牙**开关
- 建议连接同一个局域网以提升传输速度

### 系统设置
- 在设置中开启：**多设备协同 > 接续** 功能

## 使用方法

### 1. 触发应用接续
1. 在设备A上打开计算器应用并进行操作
2. 在设备B上，查看Dock栏或最近任务列表
3. 点击应用图标，应用会自动从设备A迁移到设备B
4. 设备B会显示与设备A完全一致的应用状态

### 2. 迁移数据说明
当前实现会迁移以下数据：
- 当前页面路径（主页、计算器页面、游戏页面等）
- 应用状态数据（通过AppStorage存储的数据）

### 3. 在页面中支持迁移

如果需要在特定页面支持迁移，可以在页面的生命周期中设置：

```typescript
// 在页面的 aboutToAppear 中设置当前页面路径
aboutToAppear() {
  AppStorage.setOrCreate('currentPage', 'pages/YourPage');
}

// 在页面的 onPageShow 中启用迁移能力
onPageShow() {
  let context = getContext(this) as common.UIAbilityContext;
  context.setMissionContinueState(AbilityConstant.ContinueState.ACTIVE, (result) => {
    console.info('Migration enabled');
  });
}
```

## 数据传输限制

- 通过 wantParam 传输的数据需要控制在 **100KB** 以下
- 大数据量（如图片、文件）需要使用分布式数据对象或分布式文件系统
- 当前实现适合传输页面状态和简单的应用数据

## 测试方法

### 1. 白名单测试（推荐）
在AppGallery Connect中添加测试设备到白名单，可以快速验证功能。

### 2. 真机测试
准备两台HarmonyOS设备：
1. 登录同一华为账号
2. 打开WLAN和蓝牙
3. 安装相同版本的应用
4. 按照上述使用方法进行测试

## 注意事项

1. **迁移连续性**：为了保证迁移后的应用依然可以迁移回源端，已在onCreate和onNewWant中设置迁移状态为ACTIVE

2. **页面恢复**：迁移启动时会触发onWindowStageRestore而不是onWindowStageCreate，确保在此方法中执行必要的初始化

3. **数据同步**：应用数据通过AppStorage进行管理，迁移时会自动同步到目标设备

4. **错误处理**：如果迁移失败，应用会自动回退到主页

## 扩展功能

### 支持更多迁移数据
可以在 `onContinue()` 方法中添加更多需要迁移的数据：

```typescript
onContinue(wantParam: Record<string, Object>): AbilityConstant.OnContinueResult {
  // 添加自定义数据
  wantParam['customData'] = yourCustomData;
  
  return AbilityConstant.OnContinueResult.AGREE;
}
```

### 动态控制迁移能力
可以在特定场景下禁用迁移：

```typescript
// 禁用迁移
context.setMissionContinueState(AbilityConstant.ContinueState.INACTIVE);

// 启用迁移
context.setMissionContinueState(AbilityConstant.ContinueState.ACTIVE);
```

## 技术支持

如有问题，请参考：
- [HarmonyOS官方文档 - 应用接续](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/app-continuation)
- [HarmonyOS官方文档 - 自由流转](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/free-interaction)

---

**实现完成！** 您的计算器应用现在支持自由流转功能，可以在不同HarmonyOS设备间无缝迁移。
